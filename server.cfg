sv_useDirectListing false # As it
sv_forceIndirectListing true # As it
sv_proxyIPRanges "1.1.1.1/32" # The IP ranges of your proxies (both HTTP and UDP/TCP).
sv_endpoints "1.1.1.1:30120" # 1.1.1.1:30120, 2.2.2.2:30120 the ip's of your servers (or game proxies (UDP/TCP))
sv_listingIpOverride "1.1.1.1:443" # Your domain name (always end :443 so it uses https)
sv_listingHostOverride "fivem.imperorp.net:443" # The same, but a 2nd time
fileserver_remove ".*"
fileserver_add ".*" "https://fivem.imperorp.net/files" # The URL of your file server (often the same subdomain.domain as above)
fileserver_list
set adhesive_cdnKey "" # The CDN key (As you want it to be)